# راهنمای لوگو و فاوآیکون لیستیا

## فایل‌ها را کجا بگذارم؟

همه تصاویر برند در پوشه **`static/`** کنار `style.css` و `app.js` قرار می‌گیرند:

```
static/
├── logo.png                 ← فایل اصلی لوگو (منبع؛ مربعی، حداقل ۵۱۲×۵۱۲)
├── favicon.png              ← نسخه بزرگ فاوآیکون (اختیاری، برای سازگاری قدیمی)
├── logo-192.png             ← ساخته‌شده خودکار — لوگوی سایدبار و صفحه ورود
├── logo-512.png             ← ساخته‌شده خودکار — آیکون نصب روی موبایل (PWA)
├── apple-touch-icon.png     ← ساخته‌شده خودکار — آیکون آیفون/آیپد (۱۸۰×۱۸۰)
├── favicon-32.png           ← ساخته‌شده خودکار — آیکون تب مرورگر
├── favicon-16.png           ← ساخته‌شده خودکار
└── favicon.ico              ← ساخته‌شده خودکار (چندسایزه: ۱۶/۳۲/۴۸/۶۴)
```

## تعویض لوگو در سه قدم

۱. فایل جدید را با نام **`logo.png`** در پوشه `static/` جایگزین کنید (مربعی و ترجیحاً ۵۱۲×۵۱۲ یا بزرگ‌تر، پس‌زمینه شفاف هم اشکالی ندارد).

۲. آیکون‌های کوچک را دوباره بسازید:

```bash
pip install pillow          # فقط یک‌بار
python tools/make_icons.py  # از static/logo.png می‌خواند و بقیه را می‌سازد
```

اگر فایل شما نام دیگری دارد: `python tools/make_icons.py masir/logo-jadid.png`

۳. تغییرات را کامیت و دیپلوی کنید. تمام.

> اگر Pillow ندارید، می‌توانید فایل‌های بالا را دستی (با هر ابزار گرافیکی) با همان نام‌ها و اندازه‌ها بسازید و در `static/` بگذارید.

## چرا قبلاً لوگو در اپ دیده نمی‌شد؟

سه دلیل رایج که همگی برطرف شده‌اند:

1. **کش یک‌ساله مرورگر:** استاتیک‌ها با `?v=` نسخه‌گذاری می‌شوند، ولی این نسخه فقط از روی `style.css` و `app.js` ساخته می‌شد. پس با عوض‌کردن لوگو، آدرس عوض نمی‌شد و مرورگر همان تصویر کش‌شده قدیمی را نگه می‌داشت. حالا اندازه و تاریخ خود فایل‌های لوگو/فاوآیکون هم در نسخه می‌آید.
2. **حجم زیاد:** لوگوی اصلی ۱٫۹ مگابایت و فاوآیکون ۱ مگابایت بود؛ برای تصویری که ۳۸ پیکسل نمایش داده می‌شود. حالا نسخه ۳۶ کیلوبایتی استفاده می‌شود و آیکون تب ۲ کیلوبایت است.
3. **مسیر `/static` روی بعضی هاست‌ها:** آیکون‌ها علاوه بر `/static/...` از ریشه هم سرو می‌شوند:
   `/favicon.ico` · `/favicon-32.png` · `/apple-touch-icon.png` · `/logo-192.png` · `/logo-512.png`
   پس حتی اگر هاست مسیر `/static/*` را جای دیگری بفرستد، آیکون‌ها نمایش داده می‌شوند. اگر تصویر بهینه پیدا نشود، خودکار به `logo.png` اصلی برمی‌گردد.

## نصب روی موبایل (Add to Home Screen)

فایل `/manifest.webmanifest` به‌صورت خودکار توسط اپ ساخته می‌شود و لوگوی ۱۹۲ و ۵۱۲ را معرفی می‌کند؛
بنابراین وقتی کاربر «افزودن به صفحه اصلی» را بزند، آیکون لیستیا با همین لوگو ثبت می‌شود.

## نکته‌های مخصوص لیارا (Liara)

روی لیارا اپ با **gunicorn** اجرا می‌شود و فایل‌های استاتیک را خود اپ سرو می‌کند.
برای همین حالا از **WhiteNoise** استفاده می‌کنیم (به `requirements.txt` اضافه شده) که
فایل‌های `static/` را با هدرهای درست کش و `ETag` سرو می‌کند. اگر whitenoise نصب نشود،
اپ خطا نمی‌دهد و فلسک مثل قبل فایل‌ها را سرو می‌کند.

بعد از دیپلوی، برای عیب‌یابی این آدرس‌ها را مستقیم در مرورگر باز کنید:

```
https://<آدرس-اپ-شما>.liara.run/logo-192.png
https://<آدرس-اپ-شما>.liara.run/favicon.ico
https://<آدرس-اپ-شما>.liara.run/static/logo-192.png
```

- اگر **هر سه** باز شدند ولی در صفحه دیده نمی‌شوند → مسئله فقط کش مرورگر است (Hard Refresh).
- اگر آدرس‌های ریشه باز شدند ولی `/static/...` نه → مشکل از مسیر static هاست است و چون
  قالب‌ها از آدرس ریشه استفاده می‌کنند، لوگو دیگر نمایش داده می‌شود.
- اگر **هیچ‌کدام** باز نشد → فایل‌ها در دیپلوی نرفته‌اند؛ فایل `.liaraignore` (و `.gitignore`)
  را بررسی کنید که پوشه `static/` یا `*.png` را نادیده نگیرد، و مطمئن شوید فایل‌ها کامیت شده‌اند.

اگر `liara.json` دارید، چیزی برای static لازم نیست تغییر کند؛ فقط مطمئن شوید ماژول اپ درست است:

```json
{
  "platform": "flask",
  "port": 8000,
  "flask": { "appModule": "app:app", "timezone": "Asia/Tehran" }
}
```

## اگر بعد از تعویض، هنوز لوگوی قدیمی را می‌بینید

- یک‌بار **Hard Refresh** بزنید: در ویندوز `Ctrl+Shift+R` و در مک `Cmd+Shift+R`.
- در موبایل، آیکون صفحه اصلی را حذف و دوباره اضافه کنید (سیستم‌عامل آیکون را جدا کش می‌کند).
- مطمئن شوید فایل‌های `static/*.png` واقعاً کامیت و دیپلوی شده‌اند: `git status` نباید آن‌ها را «حذف/تغییرنیافته» نشان دهد.
