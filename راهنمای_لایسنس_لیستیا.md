# راهنمای سیستم لایسنس و تغییرات نرم‌افزار «لیستیا»
**طراحی و توسعه: س.م.قتالی**

---

## ۱. تغییرات نام و هویت بصری
* **نام نرم‌افزار:** در تمامی بخش‌های برنامه (عنوان صفحات، هدر، منوی کناری، فرم ورود و ثبت‌نام، فایل اکسل نمونه و...) نام **«لیستیا»** با نشان اختصاصی **«ل»** جایگزین شد.
* **شناسه توسعه‌دهنده:** عبارت **«طراحی و توسعه: س.م.قتالی»** در فوتر منوی کناری (سایدبار)، صفحه ورود و ثبت‌نام، پنجره فعال‌سازی لایسنس و صفحه مدیریت حساب کاربری اضافه شد.

---

## ۲. منطق سیستم لایسنس (نسخه آزمایشی در برابر نسخه کامل)
* **کاربر جدید (نسخه آزمایشی / رایگان):**
  * حداکثر مجاز به ثبت **۱ تأمین‌کننده** و **۵ محصول** است.
  * به محض تکمیل این ظرفیت:
    1. فرم‌های ثبت سریع، ثبت خرید جدید، افزودن تأمین‌کننده و ایمپورت اکسل برای موارد اضافی قفل می‌شوند.
    2. پیام هشدار و پنجره مودال لایسنس نمایش داده می‌شود:
       > «سقف نسخه آزمایشی تکمیل شده است. برای ثبت موارد بیشتر باید لایسنس لیستیا را تهیه کنید.»
    3. کاربر می‌تواند با همان **۱ تأمین‌کننده و ۵ محصول فعلی** به صورت نامحدود کار کند (سفارش‌گذاری، آرشیو، تغییر وضعیت، ویرایش مشخصات، تغییر تعداد و جستجو).
* **کاربر فعال‌شده با لایسنس (نسخه کامل و نامحدود):**
  * تمامی محدودیت‌ها حذف شده و کاربر می‌تواند بی‌نهایت تأمین‌کننده و محصول ثبت کند.
  * برچسب وضعیت به `💎 نسخه کامل لیستیا (لایسنس فعال)` تغییر می‌یابد.

---

## ۳. نحوه ساخت و صدور لایسنس برای مشتریان

یک اسکریپت آماده و تعاملی به نام `generate_license.py` در ریشه پروژه قرار گرفته است.

### روش‌های اجرا:

#### الف) اجرای تعاملی (ساده‌ترین روش):
```bash
python3 generate_license.py
```
سپس نام کاربری یا شناسه فعال‌سازی مشتری (که در صفحه حساب کاربری یا پنجره فعال‌سازی لایسنس به او نمایش داده شده) را وارد کنید. کلید لایسنس همراه با متن آماده برای ارسال به مشتری نمایش داده می‌شود.

#### ب) اجرای مستقیم با نام کاربری یا کد مشتری:
```bash
python3 generate_license.py username
# یا با کد فعال‌سازی مشتری:
python3 generate_license.py LST-2C17-F3FA
```

#### ج) ساخت کلید سراسری (Master Key):
اگر بخواهید یک کلید مستر بسازید که روی هر حساب کاربری کار کند:
```bash
python3 generate_license.py --master
```

---

## ۴. نحوه فعال‌سازی لایسنس توسط مشتری
1. مشتری وارد حساب کاربری خود در «لیستیا» می‌شود.
2. روی بخش **«نسخه آزمایشی»** در سایدبار کلیک می‌کند یا به صفحه **«حساب من»** می‌رود.
3. کلید لایسنس دریافت شده (به فرمت `LST-XXXX-XXXX-XXXX-XXXX`) را در کادر مربوطه وارد کرده و دکمه **«ثبت و فعال‌سازی لایسنس»** را می‌زند.
4. پیام موفقیت نمایش داده شده و تمام قفل‌ها بلافاصله باز می‌شوند.
